# API keys are configured via the web UI settings page or config.toml.
# The backend auto-creates config.toml from sample.config.toml on first run.
# DuckDuckGo search works without any API key.
# For cloud LLM providers, enter keys in the UI after startup.
# For local LLMs, Ollama is included in the compose and available at http://ollama-service:11434.
